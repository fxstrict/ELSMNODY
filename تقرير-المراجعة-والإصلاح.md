# تقرير مراجعة وإصلاح مشروع ELSMNODY BESPOKE

**تاريخ المراجعة:** 25 أغسطس 2026

---

## 1. ملخص تنفيذي

المشروع تم فحصه بالكامل (كل ملفات HTML وCSS وJS و Code.gs)، واتلقت 4 مشاكل حقيقية، مش مجرد ملاحظات شكلية:

| # | المشكلة | الخطورة | الحالة |
|---|---|---|---|
| 1 | خطوة "المقاسات" (booking wizard) مش قابلة للوصول من أي صفحة تقريباً في الموقع | 🔴 عالية | ✅ تم الإصلاح |
| 2 | صفحة `collections.html` بتعرض شبكة فاضية تماماً — من غير أي كروت مجموعات | 🔴 عالية | ✅ تم الإصلاح |
| 3 | صفحتَي `blazer.html` و `tuxedo.html` كان بيتم مسح محتواهم الحقيقي واستبداله بمحتوى "Executive" عند كل تحميل | 🔴 عالية | ✅ تم الإصلاح |
| 4 | كل صور og:image والروابط الاعتمادية (fallback) في الموقع معتمدة على GitHub الشخصي لمطوّر تاني (`fxstrict`) بدل ملفات المشروع | 🟠 متوسطة | ✅ تم الإصلاح |

---

## 2. مشكلة "المقاسات" — التشخيص الكامل

خطوة "المقاسات" (Measurements) **مش صفحة مستقلة** — هي **الخطوة الثالثة (step3)** جوه معالج الحجز متعدد الخطوات (booking wizard) اللي موجود **فقط** داخل `contact.html`، ومبني بواسطة `js/booking.js`.

المشكلة إن كل زرار "احجز موعدك" في الموقع (باستثناء صفحة المعرض) كان بيوجّه لمكان تاني تماماً:

| الملف | كان بيوجّه لـ | المشكلة |
|---|---|---|
| `js/home.js` (index.html) | `#contact` | قسم واتساب/تليفون بسيط جوه index.html — مفيهوش معالج حجز خالص، ولا خطوة مقاسات |
| `js/about.js` | `index.html#contact` | نفس المشكلة |
| `js/fabrics.js` | `index.html#booking` | **anchor مش موجود أصلاً** في index.html — رابط ميت 100% |
| `js/collections.js` | `../index.html#contact` | نفس المشكلة + خطأ إضافي (تفصيل تحت) |
| `js/gallery.js` | `contact.html` | ✅ الوحيد الصحيح |

**النتيجة:** أي زائر يدخل من الصفحة الرئيسية أو صفحة الأقمشة أو "عن المصمم" أو أي صفحة مجموعة، ميقدرش يوصل لمعالج الحجز الحقيقي (وبالتالي لخطوة المقاسات) إلا لو كتب `contact.html` يدوي في المتصفح، أو دخل أول من صفحة المعرض.

### الإصلاح
كل أزرار "احجز موعدك" (nav_book) في `home.js` و `about.js` و `fabrics.js` و `collections.js` بقت توجّه فعلياً لـ `contact.html`.

---

## 3. باغ إضافي مكتشف: شبكة المجموعات فاضية بالكامل

أثناء تتبع مشكلة المقاسات، لقيت إن `collections.html` (صفحة قائمة كل المجموعات) فيها:

```html
<div class="collections-index-grid" id="ci-grid">
  <!-- Injected by JS -->
</div>
```

لكن **مفيش أي دالة JS في المشروع كله** كانت بتستهدف `#ci-grid`، ومفيش أي CSS اسمها `.collections-index-grid` في `css/collections.css`. يعني الصفحة كانت بتفتح فاضية تماماً — من غير أي كارت لأي مجموعة (تنفيذي، أفراح، قضائية، Black Label، سموكن، بليزر).

### الإصلاح
- أضفت دالة `renderIndexGrid()` في `js/collections.js` بتبني 6 كروت (كل المجموعات الست، شاملة blazer وtuxedo اللي معندهاش بيانات في `COLLECTIONS_DATA` لأن صفحاتهم مبنية يدوياً).
- أضفت CSS كامل (`.collections-index-grid` وتوابعها) في نهاية `css/collections.css` بنفس أسلوب التصميم المستخدم في باقي الموقع.

---

## 4. باغ خطير تاني: محتوى blazer.html و tuxedo.html بيتمسح

`js/collections.js` نفس الملف بيتحمّل على 6 صفحات مختلفة: `collections.html` + 6 صفحات تفاصيل. من ضمنهم:
- 4 صفحات (`executive`, `wedding`, `judicial`, `black-label`) مبنية بـ **قوالب فاضية** (`<section id="coll-hero"></section>` بدون محتوى) وبتتملى ديناميكياً من `COLLECTIONS_DATA`.
- صفحتين (`blazer.html`, `tuxedo.html`) مبنيتين **يدوياً بمحتوى ثابت وكامل** — ومعندهمش بيانات في `COLLECTIONS_DATA` أصلاً.

الدالة القديمة `detectCollection()` كانت لما متلاقيش الصفحة في `COLLECTIONS_DATA`، بترجع `'executive'` كـ fallback بدل ما ترجع `null`. يعني على `blazer.html`، كان الكود بياخد بيانات "Executive" ويحقنها جوه `#coll-hero` — واللي هو نفس الـ id الموجود في `blazer.html` — فيمسح العنوان والوصف الحقيقي بتاع البليزر ويحطله محتوى تنفيذي غلط، في كل مرة تتحمل فيها الصفحة!

### الإصلاح
عدّلت `renderPage()` عشان لو `COLLECTIONS_DATA[cKey]` مش موجودة، يوقف فوراً بعد الناف بار والفوتر، ومايلمسش أي محتوى ثابت موجود بالفعل.

---

## 5. خطأ المسارات النسبية في collections.js (`../`)

`js/collections.js` بيتحمّل من مكانين مختلفين في العمق:
- `collections.html` (في جذر المشروع — عمق صفر)
- `collections/*.html` (في مجلد فرعي — عمق واحد)

الكود كان مثبت المسار `'../'` بشكل دائم (في متغير `IMG` وفي كل روابط الناف بار)، وده صحيح فقط لصفحات التفاصيل الفرعية. لما نفس الكود يشتغل على `collections.html` نفسها، كان بيحوّل كل روابط القائمة (المجموعات، رحلة الخياطة، الأقمشة، عن المصمم، المعرض، التواصل) لمسار غلط بيطلع برّه المشروع (`../index.html` وهي فعلياً برّه فولدر الموقع).

### الإصلاح
أضفت كشف تلقائي للعمق:
```js
const ON_DETAIL_PAGE = /\/collections\//.test(window.location.pathname);
const DEPTH = ON_DETAIL_PAGE ? '../' : '';
```
وكل الروابط والمسارات بقت تستخدم `${DEPTH}` بدل `'../'` الثابتة.

---

## 6. روابط ووصلات GitHub الخارجية — تحويلها لملفات محلية

المشروع كان فيه اعتماد كامل على مستودع GitHub شخصي (`fxstrict/ELSMNODY`) بدل ملفات المشروع نفسه، في 3 صور من الاستخدام:

### أ) وسوم `og:image` و `canonical` (15 وسم في 11 ملف HTML)
كانت بتشاور مباشرة على:
```
https://raw.githubusercontent.com/fxstrict/ELSMNODY/70ce58e3bf6f77fe707949858f531bbeb6d7fe0c/...
https://fxstrict.github.io/ELSMNODY/...
```
ده معناه إن أي معاينة مشاركة (WhatsApp / فيسبوك) أو نتيجة بحث جوجل، كانت هتعتمد على commit قديم محدد (`70ce58e...`) في ريبو شخصي لمطوّر تاني — لو الريبو ده اتمسح أو بقى private أو الـ commit اتشال، كل صور المعاينة في الموقع هتتكسر فجأة من غير أي تحذير.

**تم الإصلاح:** استبدلت كل الروابط دي بـ `https://elsmnody.com/...` (بناءً على نطاق البريد الإلكتروني الموجود بالفعل في إعدادات العلامة `info@elsmnody.com`).

> ⚠️ **ملحوظة مهمة:** `elsmnody.com` ده **افتراض** مبني على إيميل البراند الموجود في الكود — مش نطاق مؤكد. لازم تراجع/تؤكد النطاق الفعلي اللي هيتنشر عليه الموقع، وتحدّثه في مكان واحد بس: `BRAND.siteUrl` في أول ملف `js/seo.js` (سطر 23). كل الصفحات هتاخد القيمة الصح تلقائياً منه.

### ب) `js/seo.js` — متغير `imageBase` ميت
كان فيه متغير `imageBase` بيشاور على نفس رابط GitHub الشخصي، **لكنه مش مستخدم في أي مكان في الكود خالص** (كود ميت). تم حذفه بالكامل. دالة `absUrl()` أصلاً بتبني الروابط المطلقة من `BRAND.siteUrl` بس، فمفيش أي تأثير وظيفي.

### ج) صور تحمّل مباشرة من GitHub (بدل الملفات المحلية)
| الملف | كان بيحصل | الإصلاح |
|---|---|---|
| `js/fabrics.js` | صور "رحلة القياس/القص" بتتحمل من GitHub بامتداد **`.png`** غلط، والملف المحلي الفعلي `.webp` | استخدام الملف المحلي الصحيح مباشرة |
| `js/booking.js` | صور اختيار القماش (fabric selector) في الخطوة 3، بتحاول تحميل من GitHub بامتداد `.png` أولاً (طلب شبكة زيادة كل مرة، وهيفشل لأن الامتداد أصلاً غلط)، وبعدين تتراجع (`onerror`) للملف المحلي `.webp` | استخدام الملف المحلي مباشرة من غير أي محاولة خارجية |
| `js/gallery.js` | لو صورة محلية فشلت في التحميل، `onerror` كان بيحاول يجيبها من GitHub | إزالة الاعتماد الخارجي؛ لو الصورة المحلية مفقودة فعلاً، الصورة بتتخفي بدل ما تحمّل من مصدر خارجي غير موثوق |

### د) `js/whatsapp.js`
تعليق التوثيق في أول الملف كان بيقترح استخدام مسارات مطلقة من الجذر (`/css/whatsapp.css`, `/js/whatsapp.js`) وهي مختلفة عن الاستخدام الفعلي في كل صفحات الموقع (اللي بيستخدم مسارات نسبية). عدّلت التعليق بس عشان ميضلّلش أي تعديل مستقبلي — الاستخدام الفعلي في كل الصفحات سليم بالفعل.

---

## 7. ملف Code.gs (Google Apps Script) — إيه هو وليه موجود

### السبب
الموقع بيستخدم **Google Apps Script** كـ backend مجاني بدون سيرفر (serverless) لاستقبال بيانات الحجز من `contact.html`. ده بديل عملي لعمل سيرفر Node/PHP كامل، مبني بالكامل جوه بيئة Google المجانية.

### آلية العمل
1. العميل بيملى نموذج الحجز في `contact.html` (4 خطوات: الخدمة → التخصيص → المقاسات → المراجعة والإرسال).
2. `js/booking-api.js` بيبعت البيانات (JSON) كـ POST request لـ URL بتاع Apps Script المنشور (`SCRIPT_URL` — لسه Placeholder: `REPLACE_WITH_YOUR_APPS_SCRIPT_WEB_APP_URL`).
3. `Code.gs` (اللي هيشتغل على سيرفرات جوجل بعد النشر) بيستقبل الطلب في `doPost(e)` وبيعمل 4 حاجات بالترتيب:
   - **يحفظ الحجز** في Google Sheet (دالة `saveToSheets`) — بيولّد `bookingId` فريد، وبيسجل كل بيانات العميل والمقاسات (صدر/كتف/خصر/ورك/ذراع... إلخ) في صف جديد.
   - **يرفع أي ملفات** (صورة/فيديو/PDF للمقاسات لو العميل رفعها) لمجلد مخصص في Google Drive باسم العميل، وبيخلي الملف قابل للمشاهدة بالرابط (`uploadFilesToDrive`).
   - **يبعت إيميل تأكيد للعميل** (`sendClientConfirmation`) بتصميم HTML بالعربي أو الإنجليزي حسب لغة الحجز.
   - **يبعت إيميل تنبيه للإدارة** (`sendAdminNotification`) فيه كل تفاصيل الحجز وروابط الملفات المرفوعة.
4. `doGet(e)` موجودة بس عشان تأكيد إن الـ Apps Script شغّال (health check) لو حد فتح رابط الـ Web App مباشرة في المتصفح.

### الميزات اللي هتشتغل فعلياً لو الملف اترفع واتظبط
- **حجوزات بتتسجل تلقائياً** في جدول Google Sheets منظم (بدون أي تدخل يدوي)، بترويسة ملونة وأعمدة مجهزة (28 عمود شامل كل بيانات العميل والمقاسات والتخصيص).
- **أرشفة تلقائية للملفات** (صور/فيديوهات المقاسات) في Google Drive، كل عميل بمجلد خاص باسمه ورقم حجزه.
- **إيميل تأكيد فوري للعميل** بعد إرسال الحجز.
- **تنبيه فوري للإدارة** بكل حجز جديد، بروابط مباشرة للملفات المرفوعة.
- **معرّف حجز فريد (Booking ID)** لكل عميل، يتسجل في كل من الشيت والإيميلات، عشان سهولة المتابعة.

### إيه اللي لسه ناقص عشان الميزات دي تشتغل (موجود بالفعل في `docs/setup-google.md`)
1. نشر `Code.gs` كـ **Web App** من داخل محرر Google Apps Script (Deploy → New deployment → Web app).
2. تحديث 3 قيم في أول `Code.gs`:
   - `SPREADSHEET_ID` — معرّف الشيت اللي هيتسجل فيه الحجوزات
   - `DRIVE_FOLDER_ID` — معرّف مجلد Drive اللي هترفع فيه الملفات
   - `ADMIN_EMAIL` — إيميل الإدارة اللي هيوصله التنبيه
3. تشغيل دالة `setupInitialSheet()` مرة واحدة بس من داخل المحرر (بتجهّز ترويسة الشيت أول مرة).
4. تحديث `SCRIPT_URL` في `js/booking-api.js` (سطر 16) بالرابط اللي جوجل هيديه بعد النشر.

**من غير الخطوات دي، نموذج الحجز في `contact.html` هيفضل شغال بصرياً (frontend) لكن مش هيقدر يحفظ أي بيانات فعلياً** — أي محاولة إرسال هترجع خطأ لأن `SCRIPT_URL` لسه placeholder.

---

## 8. ملخص كل الملفات اللي اتعدلت

| الملف | التعديل |
|---|---|
| `js/seo.js` | تحديث `BRAND.siteUrl`، حذف `imageBase` الميت |
| `js/collections.js` | كشف عمق ديناميكي (`DEPTH`)، إصلاح كل الروابط، إضافة `renderIndexGrid()`، حماية `blazer.html`/`tuxedo.html` من المسح، إصلاح 4 روابط صور `branding/*.png` → `.webp` |
| `js/fabrics.js` | إزالة `RAW` (GitHub)، إصلاح روابط `index.html#booking` → `contact.html` |
| `js/booking.js` | إزالة `IMG_BASE` الخارجي، استخدام صور محلية `.webp` |
| `js/gallery.js` | إزالة `RAW` (fallback خارجي) |
| `js/home.js` | إصلاح 3 أزرار حجز → `contact.html` |
| `js/about.js` | إصلاح 3 أزرار حجز → `contact.html` |
| `js/whatsapp.js` | تصحيح تعليق التوثيق (مسارات نسبية) |
| `css/collections.css` | إضافة تنسيق `.collections-index-grid` الكامل |
| `css/about.css` | إصلاح باغ overflow أفقي في تايم لاين "رحلة عقدين" (تفصيل في القسم 10) |
| `css/design-system.css` | إضافة `overflow-x: hidden` كشبكة أمان عامة على `html, body` |
| `index.html`, `about.html`, `fabrics.html`, `gallery.html`, `collections/tuxedo.html`, `collections/blazer.html`, `blog/*.html` (5 ملفات) | استبدال روابط GitHub الخارجية في `og:image`/`canonical` بروابط `elsmnody.com` |
| `PROJECT_STATE.json` | تحديث `site_url` |

---

## 10. جولة مراجعة تانية: صور تالفة + انحراف صفحة "عن المصمم"

بعد الإصلاحات السابقة، تمت مراجعة تانية شاملة بعد فحص صور فعلية من الموقع شغّال — اتلقت مشكلتين حقيقيتين تانيين:

### أ) 4 صور تالفة (امتداد غلط) — السبب الحقيقي وراء أيقونات الصور المكسورة

عملت فحص آلي كامل لكل رابط صورة في المشروع (HTML + JS + CSS) قابلته بكل ملف صورة موجود فعلياً على القرص. النتيجة: **4 روابط بالظبط** كانت بامتداد `.png` بينما الملف الفعلي على القرص `.webp` — كلهم في `js/collections.js`، جوه مصفوفات `gallery` بتاعة صفحات المجموعات:

| الرابط الغلط | الملف الفعلي | مستخدم في |
|---|---|---|
| `branding/branding-packaging.png` | `branding-packaging.webp` | معرض مجموعة الأفراح + Black Label |
| `branding/branding-label.png` | `branding-label.webp` | معرض المجموعة القضائية |
| `branding/branding-card.png` | `branding-card.webp` | معرض Black Label |
| `branding/branding-atelier-sign.png` | `branding-atelier-sign.webp` | معرض Black Label |

ده بالظبط سبب أيقونة الصورة المكسورة اللي ظهرت في صفحة المجموعة القضائية (والأفراح وBlack Label) — الصورة الأساسية (الغلاف) سليمة 100%، لكن الصور الإضافية جوه معرض كل مجموعة كانت بتحاول تحمّل بامتداد مش موجود. تم تصحيح الأربعة.

**تأكيد بعد الإصلاح:** شغّلت الموقع فعلياً (headless browser) وفحصت كل صورة `<img>` في 9 صفحات رئيسية (index، collections، judicial، wedding، black-label، about، fabrics، gallery، contact) — **صفر صور مكسورة** في كل الصفحات.

### ب) انحراف/تمدد أفقي في صفحة "عن المصمم" — تايم لاين "رحلة عقدين"

السبب الحقيقي (اتأكد بفحص فعلي للصفحة على عرض شاشة موبايل 390px):

قسم التايم لاين بيستخدم تخطيط "يمين/يسار" متبادل، وفيه قاعدة CSS مخصصة للعربي (RTL) بتحدد هامش الكروت:
```css
[dir="rtl"] .about-timeline__item--right { margin-inline-start: 50%; }
```
وفيه قاعدة تانية جوه `@media (max-width: 700px)` مفروض تصفّر الهامش ده على الموبايل (`margin-inline-start: 0`) عشان الكروت تترص عمود واحد. **المشكلة:** القاعدة الأولى (بتاعة RTL) عندها أولوية CSS (specificity) أعلى من قاعدة الموبايل، فكانت **فاضلة شغالة حتى على الشاشات الصغيرة** — يعني كل كارت من نوع "يمين" (2007، 2017...) كان لسه شايل هامش 50% من عرض الحاوية، مع عرض 100% في نفس الوقت، فالنتيجة إجمالي عرض= 150% من الحاوية → **تمدد أفقي حقيقي بمقدار ~147px خارج حدود الشاشة**، وده بالظبط سبب قطع النص من الجهة اليسار في اللقطة اللي بعتّها.

**الإصلاح:**
1. أضفت قاعدة CSS بنفس مستوى الأولوية (نفس الـ specificity) بس بعدها في ترتيب الملف، عشان تكسب وتصفّر الهامش فعلياً على الموبايل.
2. أضفت `overflow-x: hidden` على `html` و `body` كشبكة أمان عامة في كل الموقع — حتى لو ظهر خطأ مشابه في أي صفحة تانية مستقبلاً، الصفحة مش هتتمدد أفقياً أو تدي سكرول جانبي، هتتقص بس بدل ما "تكبر حجم الصفحة" زي ما وصفت.

**تأكيد بعد الإصلاح:** قسته فعلياً — `scrollWidth` بقى **مطابق تماماً** لعرض الشاشة (390 = 390) بعد ما كان 537px (يعني كان في تمدد أفقي حقيقي بمقدار 147px). التايم لاين دلوقتي بيترص عمود واحد نضيف من غير أي قطع أو تمدد.

---

## 11. تحديث ملخص الملفات المعدّلة (الجولة التانية)

| الملف | التعديل الإضافي |
|---|---|
| `js/collections.js` | إصلاح 4 روابط صور `branding/*.png` → `.webp` |
| `css/about.css` | إصلاح باغ specificity في `[dir="rtl"] .about-timeline__item--right` اللي كان بيكسر الموبايل |
| `css/design-system.css` | `overflow-x: hidden` على `html, body` |

---

## 9. حاجات تستاهل مراجعة (مش أخطاء، لكن ملاحظات هندسية)

- **ازدواجية القوالب في صفحات المجموعات:** 4 صفحات (executive/wedding/judicial/black-label) مبنية بنظام بيانات مركزي (`COLLECTIONS_DATA`)، بينما صفحتين (blazer/tuxedo) مبنيين يدوياً بالكامل. ده شغال دلوقتي بعد الإصلاح، بس أي تعديل مستقبلي (سعر، وصف، صور) هيحتاج يتعمل مرتين بطريقتين مختلفتين لو حبيت توحد النظام مستقبلاً.
- **نطاق `elsmnody.com` افتراضي:** لازم تأكيد النطاق الحقيقي وتحديثه في `js/seo.js` (سطر 23) قبل النشر النهائي.
- **`google-apps-script/Code.gs` لسه محتاج نشر يدوي:** مينفعش يتنشر تلقائياً من هنا لأنه محتاج حساب Google الخاص بيك (Sheet ID / Drive Folder ID / صلاحيات).
